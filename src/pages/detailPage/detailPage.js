import React from 'react';
import './detailPage.scss';

import Page from '../../components/page';

const DetailPage = ({handleToggle, toggle}) => {

    return (
        <div></div>
    );
};

export default DetailPage;